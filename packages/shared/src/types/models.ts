export interface Profile { id: string; email: string; role: AppRole; first_name: string; last_name: string; phone?: string | null; avatar_url?: string | null; is_active: boolean; created_at: string; updated_at: string; }
export interface PatientProfile { id: string; profile_id: string; date_of_birth?: string | null; gender: string; blood_group: string; allergies: string[]; emergency_contact_name?: string | null; emergency_contact_phone?: string | null; address?: string | null; city?: string | null; latitude?: number | null; longitude?: number | null; }
export interface DoctorProfile { id: string; profile_id: string; license_number: string; specialty: string; sub_specialties: string[]; bio?: string | null; consultation_fee: number; currency: string; verification_status: string; office_address?: string | null; office_city?: string | null; office_latitude?: number | null; office_longitude?: number | null; }
export interface Pharmacy { id: string; name: string; license_number: string; address: string; district?: string | null; city: string; latitude: number; longitude: number; phone: string; email?: string | null; opening_hours: Record<string, unknown>; is_duty_pharmacy: boolean; is_verified: boolean; is_active: boolean; }
export type AppRole = 'patient' | 'doctor' | 'pharmacy_admin' | 'pharmacist' | 'platform_admin';
